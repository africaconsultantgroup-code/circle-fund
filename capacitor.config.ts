import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sikacircle.app',
  appName: 'App name: Sika CircleApp ID: com.sikacircle.app',
  webDir: 'dist'
};

export default config;
